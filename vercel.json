{
  "$comment": "Hébergement Vercel (2026-08-11, après épuisement du quota Netlify) : assemble le site statique comme le faisait le workflow Netlify — index.html + public/* à la racine. Lu automatiquement à l'import du projet, aucune configuration manuelle à faire.",
  "buildCommand": "mkdir -p _site && cp index.html _site/ && cp -r public/. _site/",
  "outputDirectory": "_site",
  "framework": null
}
