# Plan — Phase 4 : harmonisation (2026-08-05)

Suite des Phases 1-3 (design system CSS/SVG branché sur menu.html + arena.html,
validé). Camille a précisé que `spike3d-village.html`/`spike3d-planet2.html`
sont d'anciens écrans voués à suppression — exclus explicitement de ce lot
(voir mémoire `eluminia-village-planet2-deprecated`).

## Ce lot (limité à menu.html/arena.html + fichiers du design system)

- Suppression des 14 images PNG désormais mortes (boutons/badges/panneaux/
  fonds des lots 5-6, remplacés par le CSS pur) : badge-circle-bg.png,
  btn-neutral-bg.png, collection-btn-bg.png, menu-bg-texture(.v2/.v3).png,
  panel-texture.png, play-btn-bg(-v2).png, btn-primary-fp.png,
  btn-secondary-fp.png, btn-neutral-fp.png, badge-icon-fp.png,
  panel-popup-fp.png. Zéro référence restante vérifiée avant suppression
  (grep sur tous les .html). manifest.json mis à jour (entrées marquées
  SUPPRIMÉ, pas effacées silencieusement).
- Variables `--el-brown/cream/tan/green*` mortes supprimées du `:root` de
  arena.html (menu.html l'était déjà depuis le lot précédent).
- Vérification responsive sur les 4 formats demandés (1280×720, 896×414,
  844×390, 740×360) : menu principal, écran pré-partie, écran de fin de
  manche (le plus dense). Aucun débordement horizontal, aucun élément
  coupé — le format le plus étroit (740×360) fait défiler la carte de fin
  de manche verticalement (`overflow-y: auto` déjà en place avant ce lot,
  comportement inchangé, pas une régression).

## Hors périmètre (décision de Camille)

`spike3d-village.html`/`spike3d-planet2.html` : non touchés. Conservent
leur ancienne palette et leurs assets PNG (icônes UI, objets d'inventaire)
qui restent utilisés par ce fichier — rien supprimé côté village.

## Vérification

Captures des 4 formats × 3 écrans clés, zéro erreur console. Pas de
sous-agent de revue pour ce lot (vérifications structurelles — présence de
fichiers, débordement de mise en page — pas un jugement esthétique).
